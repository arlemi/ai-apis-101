title: AI APIs 101
tagline: A brand new course on Learning Lab
description: 'Playing around with the course builder, for now. '
template:
  repo: ai-apis-101-template
  name: ai-apis-101
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
